# 다음 기능 개선 로드맵

작성일: 2026-07-08
목적: "이 에이전트를 더 효율적으로 만들려면 어떤 데이터를 더 넣으면 좋을까"에 대한 논의 결과 정리. 다음 세션에서 이어서 작업할 때 바로 시작할 수 있도록 우선순위와 기술 접근법을 남겨둠.

## 배경

지금까지 반영된 것: 카카오 생활 인프라, 네이버 뉴스/정책(키워드 기반, 부정어·태그목록 오탐 방지 포함), 국토부 아파트 매매 실거래가(MOLIT_API_KEY). 이 중 국토부 실거래가처럼 "키워드 추측"이 아니라 "공식 데이터"로 대체할 수 있는 부분이 아직 여러 개 남아있어서, 그걸 우선순위별로 정리함.

---

## 1순위: 아파트 전월세 실거래가 (전세가율)

**왜 필요한가**: 전세가율(전세가/매매가)은 실수요 강도와 갭투자 리스크를 보여주는 핵심 지표. 매매 실거래가는 이미 반영했으니, 같은 API 계열의 전월세 자료만 추가하면 됨.

**데이터 소스**: 국토교통부_아파트 전월세 실거래가 자료 (공공데이터포털)
- https://www.data.go.kr/data/15126474/openapi.do
- 매매 API(RTMSDataSvcAptTrade)와 같은 방식으로 활용신청 (data.go.kr 계정에서 신청, 승인 보통 즉시~몇 시간)
- 파라미터도 매매랑 동일한 패턴일 가능성 높음: `LAWD_CD`(법정동코드 5자리), `DEAL_YMD`(계약년월 6자리)
- ⚠️ 주의: 매매 API 때 End Point 이름(`RTMSDataSvcAptTradeDev` → 실제로는 `RTMSDataSvcAptTrade`)과 성공 코드(`00`이 아니라 `000`)가 예상과 달라서 버그가 났었음. 전월세 API도 활용신청 상세 페이지의 "End Point"와 "일반 인증키" 화면을 스크린샷으로 먼저 확인하고 시작할 것.

**구현 방법**: `market_agent/collectors/molit.py`와 거의 같은 구조로 새 모듈(`rent.py` 또는 `molit.py`에 클래스 추가) 작성.
- 응답 필드는 매매(`dealAmount`, `excluUseAr`)와 다르게 보증금(`deposit` 또는 `보증금액`), 월세(`monthlyRent`), 계약구분(전세/월세) 필드가 있을 것 (정확한 필드명은 활용신청 후 Swagger 문서에서 확인)
- 전세가율 계산: 같은 동/기간의 전세 중위가 ÷ 매매 중위가 × 100
- 새 `EvidenceItem` category 예: `"rent_data"` 또는 기존 `"market_data"`에 통합하고 tags로 구분
- `rule_engine.py`에 새 카테고리 가중치/캡 추가 필요 (market_data 추가했던 패턴 그대로 참고)
- 테스트: `test_molit.py` 패턴 그대로 (FakeClient, XML 파싱, 전세가율 계산 회귀 테스트)

**예상 난이도**: 낮음 (기존 코드 재사용 가능)

---

## 2순위: 공식 미분양 통계로 키워드 추측 대체

**왜 필요한가**: 어제 겪은 버그(미분양 없는 단지가 "미분양"으로 잘못 감점)가 정확히 이 문제. 뉴스 키워드 대신 정부 공식 통계를 쓰면 이 종류의 오탐이 구조적으로 사라짐.

**데이터 소스**: 국토교통부 통계누리(stat.molit.go.kr) "미분양주택현황보고" 또는 한국부동산원_부동산통계 조회 서비스
- https://www.data.go.kr/data/15134761/openapi.do (한국부동산원_부동산통계 조회 서비스)
- ⚠️ 정확한 조회 파라미터(어떤 통계표 코드로 미분양 현황을 조회하는지)는 아직 확인 안 됨 — 활용신청 후 API 문서를 직접 봐야 함. 국토부 실거래가처럼 깔끔한 전용 API가 아니라 "통계 조회 서비스" 안에 여러 통계표 중 하나로 들어있을 가능성이 높음.
- 시군구 단위 월별 통계라서, 지금 MOLIT 실거래가처럼 반경이 아니라 지역 단위 신호로 반영하게 될 것.

**구현 방법**: 이 API가 실제로 무엇을 제공하는지 활용신청 후 먼저 확인 필요. 확인되면 evidence 하나로 만들어서 (예: "강동구 미분양 12세대, 전월 대비 -3세대") `category="market_data"` 또는 새 카테고리로 반영.

**예상 난이도**: 중간 (API 스펙 확인부터 필요, 매매/전월세보다 불확실성 있음)

---

## 3순위: 규제지역(조정대상지역/투기과열지구) 지정 현황

**왜 필요한가**: 지금은 뉴스에 "규제"라는 단어가 있는지로만 판단하는데, 실제로는 국토부가 공식적으로 지정/해제 고시를 냅니다. 확정 정보로 바꾸면 신뢰도가 크게 올라감.

**데이터 소스**: ⚠️ 확인 결과, 이건 **공식 오픈 API가 없음**. 국토교통부 홈페이지(molit.go.kr)에 고시 형태로만 게시되고, 이택스코리아 등 민간 사이트가 이를 정리해서 보여주는 정도.

**구현 방법**: API 연동이 아니라 다음 둘 중 하나로 접근해야 함.
1. 국토부 고시 페이지를 주기적으로 확인해서 `market_agent/regulation_areas.py` 같은 파일에 지역 목록을 수동으로 갱신 (지정 현황이 자주 바뀌는 게 아니라서 현실적인 방법)
2. 스크래핑 (안정성 낮음, 권장 안 함)

**예상 난이도**: 낮음(수동 리스트 방식) — 다만 "실시간 자동 반영"은 아니고 주기적으로 사람이 갱신해야 함.

---

## 4순위: 한국은행 기준금리 (거시 맥락)

**왜 필요한가**: 개별 단지 데이터는 아니지만, 금리는 부동산 가격에 영향을 주는 가장 큰 거시 변수 중 하나. 리포트에 "지금 금리 환경" 한 줄만 추가해도 맥락이 풍부해짐.

**데이터 소스**: 한국은행 ECOS Open API
- https://ecos.bok.or.kr/api/
- data.go.kr와 별개로 한국은행에서 직접 무료 가입 + 인증키 발급 (보통 1일 이내 승인)
- 정확한 통계코드는 가입 후 ECOS 사이트의 "통계코드검색"에서 "기준금리" 검색해서 확인 필요

**구현 방법**: 단일 값 조회라서 매우 간단. `market_agent/collectors/` 안에 작은 클라이언트 하나 추가, 최신 기준금리 값을 가져와서 리포트 상단이나 `limitations`/summary 옆에 "현재 기준금리: X%" 식으로 노출. 반경/지역과 무관한 전국 공통 값이라 캐싱해서 하루 한 번만 조회해도 충분.

**예상 난이도**: 낮음

---

## 5순위: 청약 경쟁률 (신규 분양단지 한정)

**왜 필요한가**: 신규 분양 단지라면 실제 청약 경쟁률 숫자가 뉴스 키워드보다 훨씬 직접적인 수요 지표.

**데이터 소스**: 한국부동산원_청약홈 청약접수 경쟁률 및 특별공급 신청현황 조회 서비스
- https://www.data.go.kr/data/15098905/openapi.do

**구현 방법**: 단지명으로 청약 결과를 조회해서 경쟁률 숫자를 evidence로 반영. 이미 분양이 끝나고 입주 중/입주 완료된 단지에는 해당 없음 (신규 분양 단지에만 의미 있음).

**예상 난이도**: 중간

---

## 다음 세션 시작 가이드

1. **1순위(전월세)부터 시작 추천** — 기존 `molit.py` 구조를 거의 그대로 재사용할 수 있어서 가장 빠르게 결과를 볼 수 있음.
2. 시작 전 체크리스트:
   - [ ] data.go.kr에서 "국토교통부_아파트 전월세 실거래가 자료" 활용신청 (매매 때처럼 승인까지 즉시~몇 시간)
   - [ ] 승인 화면 스크린샷으로 실제 End Point 이름과 인증키 확인 (매매 때 이름이 예상과 달라서 버그 났었음 — 같은 실수 반복 주의)
   - [ ] 로컬에서 실제 키로 먼저 호출 테스트 (resultCode가 "00"인지 "000"인지도 확인)
3. 코드 작업 순서(매매 실거래가 작업 때와 동일한 패턴):
   - 클라이언트 클래스 작성 (XML 파싱, resultCode 체크)
   - 전세가율 계산 함수 작성 + 단위 테스트
   - `agent.py`에 조건부 연동 (`molit_enabled`처럼 새 설정 플래그)
   - `rule_engine.py`에 새 카테고리 가중치 반영
   - `.env.example`, README, PROJECT_NOTES.md 업데이트
   - 로컬 검증 → git commit/push → Render 환경변수 추가

## 참고 (2026-07-08 검색으로 확인한 출처)

- [국토교통부_아파트 전월세 실거래가 자료](https://www.data.go.kr/data/15126474/openapi.do)
- [한국부동산원_부동산통계 조회 서비스](https://www.data.go.kr/data/15134761/openapi.do)
- [한국은행 Open API 서비스 (ECOS)](https://ecos.bok.or.kr/api/)
- [한국부동산원_청약홈 청약접수 경쟁률 및 특별공급 신청현황 조회 서비스](https://www.data.go.kr/data/15098905/openapi.do)
- [국토교통부 조정대상지역/투기과열지구 지정 현황](https://www.molit.go.kr/policy/stable/sta_b_03.jsp)
